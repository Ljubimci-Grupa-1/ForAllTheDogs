version: "3"

services:
  users-database:
    image: "postgres:13-alpine"
    ports:
      - "5432:5432"
    environment:
        - "POSTGRES_USER=admin"
        - "POSTGRES_PASSWORD=admin"
        - "POSTGRES_DB=forallthedogs"
